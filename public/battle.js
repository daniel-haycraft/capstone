




// // el is what ever the person chooses 
// function battleMode(el){
//     //axios.get(baseURL)
//     //.then(res => {
//         let computer = []
//         let yourPokemon = []
//     yourPokemon.push(el)
//     computer.push(dummyData[1])

//     // then id find a randomizer function math.Random, probably have used it in the path. 
//     // well i don't think i need to do that, since its the elte 4 you'd battle mroe than one right? 
//     // so all id need is a new battle function to loop through each time
//     // hmm getting over whelmed now
//         // new element ='p' .textcontent says battleing...
//         //attack button
// // yourPokemon

//     let userAttack = computer[0].health -= yourPokemon[0].attack 
//     // console.log(`${computer[0].health}`) working
//     let compAttack = yourPokemon[0].health -= computer[0].attack
//     // compAttack = `${yourPokemon[0].health}`
//     // userAttack = `${computer[0].health}`
//     // console.log(`${yourPokemon[0].health}`)
//     // health.textcontent = ${dummyData[0].health} soo it can be displayed :)
//     //vice versa
//     for (let i = 0; battles.length > i; i++){
//         if (battles[i] <= 0 && yourPokemon[0].health > computer[0].health){
//              return yourPokemon
//         } else if(battles[i] <= 0 && `${yourPokemon[0].health}` < `${computer[0].health}`){ 
//             return computer
//         } 
//     }
// }
// // ^^ becuase you can click the button multiple times until it hits zero
// // when it hits zero it alerts you then the page rerolls the pokemon and splices that pokemon out of the array.. forever
// // pokemon don't die so if i have time i can worry about that lol

        

    






// record health status and run it till it breaks honestly